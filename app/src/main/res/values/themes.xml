<!-- TODO: Light Theme -->
<resources xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- Base application theme. -->
    <style name="Theme.UnofficialDolthubClient" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. The main color for consumers to associate with the app! -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_light</item>
        <item name="colorOnPrimary">@color/primary_text</item>

        <!-- Secondary brand color. The lesser used color for things like the Floating Action Buttons! -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_light</item>
        <item name="colorOnSecondary">@color/secondary_text</item>

        <!-- Status bar color. The bar with the notification icons! -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. Use ?attr/item_name to set your colors outside of here! -->
        <!-- The custom attributes are defined in attrs.xml. The colors are defined in colors.xml. -->
        <!-- Example is ?attr/repo_card_ripple_color -->

        <!-- Ripple Color For Repo Cards -->
        <item name="repo_card_ripple_color">@color/ripple_color_light</item>
        <item name="repo_card_background">@color/card_color_light</item>
    </style>

    <!-- Not Defined In Dark Theme - So Pulled From Here When Dark Mode On -->
    <style name="Theme.UnofficialDolthubClient.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Not Defined In Dark Theme - So Pulled From Here When Dark Mode On -->
    <style name="Theme.UnofficialDolthubClient.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- colorPrimary Here is The Collapsed Action Bar -->
        <item name="colorPrimary">@color/primary</item>
    </style>

    <!-- Not Defined In Dark Theme - So Pulled From Here When Dark Mode On -->
    <style name="Theme.UnofficialDolthubClient.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>